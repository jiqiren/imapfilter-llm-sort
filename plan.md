# Project Plan

## Done

- [x] Replace `curl` with native Lua HTTPS (`luasec` + `luasocket`)
- [x] Replace regex JSON with `dkjson.encode()` / `dkjson.decode()`
- [x] Configurable categories via config file
- [x] Configurable prompts driven by category descriptions
- [x] Configurable IMAP settings (server, ssl, lookback_days)
- [x] Support Responses API and Chat Completions API
- [x] `config.example.lua` in repo
- [x] Git history merged with upstream
- [x] `AGENTS.md` with project conventions
- [x] `README.md` updated with new setup instructions
